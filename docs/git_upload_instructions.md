# Git Upload Instructions

The local repository is initialized and committed during project setup.

The initialization spec mentions this remote:

```text
https://github.com/lckcjdn/Resnet_gradient
```

When remote upload is explicitly requested, use:

```bash
git branch -M main
git remote add origin https://github.com/lckcjdn/Resnet_gradient
git push -u origin main
```
